// Set process parameters
process {
    executor = "slurm"
    queue = "cpuq"
}

// Set parameters for max resources
params {
    // Resource options
    // Defaults only, expected to be overwritten
    max_memory = "16.GB"
    max_cpus = 16
    max_time = "24.h"	
}